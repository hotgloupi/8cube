#include "Shader.hpp"

#include <cube/python.hpp>

BOOST_PYTHON_MODULE(Shader)
{
	namespace py = boost::python;
	using namespace ::cube::gl::renderer;

	py::class_<
			Shader,
			ShaderPtr,
			boost::noncopyable,
			py::bases<cube::resource::Resource>
		>(
			"Shader",
			py::no_init
		)
	;
}
