#include "angle.hpp"

#include <wrappers/boost/python.hpp>

BOOST_PYTHON_MODULE(angle)
{
	namespace py = boost::python;
	using namespace cube::units;

	py::class_<Angle>("Angle")
	;

	py::def("rad", &rad);
	py::def("rad_value", &rad_value<float>);

	py::def("deg", &deg);
	py::def("deg_value", &deg_value<float>);

}
